cmake_minimum_required(VERSION 3.5)

include(ExternalProject)

project(ExtProject-${PRO_NAME} NONE)
set(CMAKE_TLS_VERIFY OFF)

ExternalProject_Add(ext-${PRO_NAME}
    PREFIX                 ""
    URL                    ${URL}
    URL_HASH               ${URL_HASH}
    SOURCE_DIR             "${SOURCE_DIR}"
    CONFIGURE_COMMAND      ""
    BUILD_COMMAND          ""
    INSTALL_COMMAND        "${INSTALL_COMMAND}"
    TEST_COMMAND           ""
    UPDATE_COMMAND         ""
    TLS_VERIFY             "OFF"
    
    ${EXT_ARGS}
)
